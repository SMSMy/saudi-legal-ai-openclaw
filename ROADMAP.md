# ROADMAP
# خارطة طريق المشروع

**Saudi Legal AI — OpenClaw Edition** — خادم MCP للاسترجاع القانوني السعودي، نقيّ الاسترجاع بلا أي مفتاح API

---

## ⚠️ إخلاء مسؤولية قانوني / Legal Disclaimer

> **بالعربية:** هذا المشروع أداة بحثية وتحليلية مساعدة **فقط**. لا يُعدّ أي مخرج ينتج عن استخدامه استشارةً قانونية، ولا يُغني عن الرجوع إلى محامٍ أو مستشار قانوني مرخّص في المملكة العربية السعودية. كل حالة قانونية فريدة وتستلزم تقديرًا متخصصًا. **استخدم هذا الإطار تحت إشراف مختص قانوني مؤهل دائمًا.**
>
> **In English:** This project is a research and analytical assistance tool **only**. No output produced through its use constitutes legal advice, nor does it replace consultation with a licensed attorney or legal advisor in the Kingdom of Saudi Arabia. Every legal situation is unique and requires specialized judgment. **Always use this framework under the supervision of a qualified legal professional.**

---

## هوية المشروع / What This Project Is

- **خادم MCP استرجاع خالص** لـOpenClaw — `saudi_legal_mcp/` بحزمة واحدة، 9 أدوات مسجلة، بلا LLM داخلي وبلا مفاتيح API
- **قاعدة معرفة نظامية سعودية**: 20 مصدراً بمنيفستات sha256، 9 مهارات قانونية، dataset مخاطر تعاقدية (53 بنداً)
- **بوابات ثقة برمجية**: `enforce_evidence`، `placeholder_dominated`، عتبة ثقة 0.7 — لا ادعاء بلا دليل داخل المستودع
- **بوابة إصدار**: `verify_release.py` + evidence bundles + اختبارات حقن أوامر

## ما ليسه هذا المشروع / What This Project Is Not

- **ليس** برنامجاً للاستشارة القانونية الآلية — `verified` (مراجعة محامٍ مرخّص) لا تزال صفراً: كل المصادر `field_tested`
- **ليس** بديلاً عن المحامي المرخّص
- **ليس** مصدراً قانونياً رسمياً — المرجع الأصيل: هيئة الخبراء (boe.gov.sa) والجريدة الرسمية (uqn.gov.sa)
- **ليس** طبقة منتج (واجهة ويب، رفع مستندات) — مؤجَّل صراحة، خارج نطاق الريبو الحالي

---

## الحالة الحالية / Current Status

```
v0.4.16 (قبل الإصدار الرسمي الأول — semver 0.4.0 في pyproject.toml)

✅ MCP Integration        اكتملت — 9 أدوات حية عبر OpenClaw،
                          مُختبَرة من بيئة نظيفة مرتين
✅ Retrieval Trust        اكتملت — بوابات الثقة + citations + release gate
✅ Packaging              اكتملت — حزمة واحدة installable، importlib.resources
✅ Adversarial (أول اختبار) اكتمل — حقن الأوامر مُثبت كبيانات لا أوامر
⚙️  Content Coverage      20 مصدراً، كلها field_tested — صفر verified
                          (تنتظر مراجعة محامٍ مرخّص — الأولوية الحقيقية)
⏸  Product Layer         مؤجَّل صراحة

الاختبارات: 109    الـeval: precision=97.2%، recall=90.3% (65 سؤالاً)
البوابات: verify_release.py أربع بوابات خضراء + CI gate
```

---

## ما أُنجز (سجل حقيقي بالنسخ) / Completed (real versioned history)

| الإصدار | الإنجاز |
|---------|---------|
| v0.2 | الهيكل الأساسي + الحوكمة + schema/enums + CI |
| v0.3 | `find_legal_provision` + `build_legal_brief` + `enforce_evidence` + manifests |
| v0.4.0 | Packaging refactor: `saudi_legal_mcp/` حزمة واحدة + console script |
| v0.4.1-4 | manifests + validation + تثبيت نظيف من GitHub |
| v0.4.3-5 | بوابات: أل التعريف aliasing، `placeholder_warning`، `placeholder_dominated` (بوابة أ) |
| v0.4.6 | **اكتشاف الكود الميت**: تسجيل الأداتين → 9 أدوات + pin `mcp<2.0.0` (كان السيرفر يتحطم بـmcp 2.0) |
| v0.4.7 | بوابة الثقة 0.7 في `search_legal_provision` + قاعدة تفاعل البوابات |
| v0.4.8-9 | `citations` مع `link_type` + صياغة عربية بشرية للحالات + `sections_index` |
| v0.4.10-12 | تطبيع عربي (تشكيل/همزات/ألف مقصورة/تاء مربوطة) + `normalize_phrase` + أول مجموعات ترادف موثقة |
| v0.4.12 | **Release Gate**: `verify_release.py` + `generate_manifests --check` + CI + evidence bundles |
| v0.4.12 | **Adversarial #1**: حقن الأوامر مُثبت بنيوياً وسلوكياً |
| v0.4.14-15 | `citation_note` + قواعد الصياغة في **docstrings** (اكتشاف: OpenClaw لا يستهلك `instructions`) |
| v0.4.16 | كلمات عامية (stopwords) + زوجا ترادف محددان + سكربت قياس substring |

---

## القرارات المعمارية والديون الموثقة / Architectural Decisions & Documented Debt

كل بند هنا **موثَّق برقمه المقاس** — لا يُفتح بلا سبب، ولا يُصلَح استباقياً.

### 📏 substring vs word-boundary matching — دَين موثَّق برقمه (v0.4.15، مؤجَّل عمداً)
> **القياس الفعلي** (scripts/audit_substring_vs_wordboundary.py، commit 2592222):
> الـeval شُغِّل مرتين — بالمنطق الحالي (substring) وبـword-boundary صارم:
> - **precision: 0 فرق** (الخلل "متساهل" لا "مضلِّل" — يرفع recall زائفاً نادراً دون إيجابيات كاذبة)
> - **recall: 0.9028 → 0.8823 (-0.0205)** — سؤالان فقط من 65 (3.08%)
> - **المنقلبان بالاسم**: arb-002 ("يُستثنى" تنقسم عند التشكيل فتطابق "ستثنى" كـsubstring)،
>   companies-001 ("الشركات" substring داخل كلمة أطول في قسم LLC)
> - **القرار:** مؤجَّل عمداً. يُعاد فتحه فقط لو تراكمت حالات مشابهة. السكربت قابل لإعادة
>   التشغيل عند أي تحديث للـcorpus لقياس هل النسبة ازدادت.

### قرار معماري v0.4.9 — حدود بوابة الثقة (معالجة مؤقتة لا إغلاق نهائي)
> بوابة 0.7 طُبقت في `search_legal_provision` كما طُلب في v0.4.7، لكنها **أُزيلت من
> `build_legal_brief`** بعد اكتشاف أنها تكبت السيناريوهات الطبيعية (الجملة الكاملة
> تخفض الثقة النسبية تحت 0.7 حتى مع وجود الجواب في القسم). يُعاد النظر فيه عند
> إغلاق بند حساب الثقة الجذري أدناه. الاختبار المرجعي:
> `test_full_question_scenario_not_gated_by_confidence`.

### 🔴 عاجل — إصلاح حساب الثقة الجذري (مفتوح، يستلزم قراراً)
> الثقة = مطابقات/كلمات تجعل أي سؤال بصيغة جملة طبيعية يفشل بلوغ 0.7 في أي
> قسم واحد. نُفِّذ حتى الآن: ترقيم، stopwords، تنوين، همزات، ألف مقصورة،
> تاء مربوطة، مجموعات ترادف موثقة — **غير كافية** للترادف والصرف الأوسع.
> الخيارات المطروحة للقرار:
>   1. Arabic stemmer (ISRI/Snowball) — إضافة dependency تكسر مبدأ "تبعية واحدة"
>   2. قاموس ترادف مصغَّر — صيانة يدوية، يُضاف فقط بتوثيق فشل استخدام فعلي
>   3. إعادة تصميم المقياس — يغيّر دلالة match_confidence ويستلزم إعادة معايرة eval كاملة
> **لا يُنفَّذ أي خيار قبل الحسم.**

### 🔴 فجوة عامية موثقة بحدودها (v0.4.16 — مسجَّلة، لا مُجدولة)
> **الحد الرياضي:** كلما طال الاستعلام العامي بكلمات غير مطابقة حرفياً، انخفضت
> الثقة بالضرورة الحسابية — بغضّ النظر عن جودة الكود. أمثلة موثقة: "ما يبي
> يجدد العقد" (أفضل قسم 0.5)، "أفصله... مكافأة نهاية خدمة" (0.27).
> **المنفَّذ:** stopwords عامية (يبي، تبي، لي، له...) + زوجا ترادف (يجدد↔تجديد،
> أفصل/فصل↔إنهاء). **اكتشافان جانبيان:** "لي"/"له" كانتا مطابقات فرعية كاذبة
> (63 و30 مرة!)؛ "أفصله" بضمير متصل لا يغطيه زوج "أفصل↔إنهاء" — تأكيد خطر
> التعميم الصرفي. **القرار:** لا انتقال تلقائي لخيار إعادة تصميم المقياس.

### تحديث v0.4.12 — مجموعات الترادف تُضاف فقط بتوثيق فشل فعلي
> الأزواج الثلاثة الأولى (الموظف↔العامل، السنة↔سنوياً/سنوية، يستحق↔تستحق)
> وُثقت من فشل استدعاء مباشر فعلي، نُفِّذت كتوسيع scoring-side (لا تضخيم
> للمقام). القاعدة: أي مجموعة جديدة تُضاف فقط بتوثيق فشل استخدام فعلي
> يحدد المصطلحين تحديداً.

### قاعدة تفاعل البوابات (v0.4.7 إلزامية)
> عند إضافة أي بوابة قرار جديدة، **أعد اختبار تفاعلها مع
> `placeholder_dominated` و`MATCH_CONFIDENCE_THRESHOLD` تحديداً** — لا اختبار
> وحدة معزول. كل البوابات تقرر على **عدد الأقسام الخام** (`prov_raw_count`)،
> لا على القائمة المُصفَّاة. الاختبار المرجعي:
> `test_gate_interaction_weak_filter_cannot_bypass_placeholder_gate`.

### قنوات الإيصال (v0.4.15 — درس مُقيس في كود العميل)
> OpenClaw **لا يستهلك** حقل `instructions` من MCP handshake (صفر مرات في
> bundles المثبّتة) — القناة الوحيدة للنص التوجيهي هي **docstrings الأدوات**
> وحقول المخرجات. أي سياسة نصية جديدة تعيش في docstrings، لا instructions.
> التفصيل الكامل في `legal_response_policy.md` §0 و`memory/`.

---

## المتبقي / Remaining Work

### 🔴 الأولوية الحقيقية: مراجعة قانونية بشرية (الطريق الوحيد لـ`verified`)
- [ ] مراجعة محتوى المصادر الـ20 من محامٍ مرخّص في المملكة
- [ ] ترقية المصادر من `field_tested` إلى `verified` (بمعايير `legal_response_policy.md`)
- [ ] ترقية بنود dataset من `draft`/`field_tested` إلى `verified`
- [ ] إغلاق علامات `[يحتاج تحقق]` المتبقية في المصادر (135 علامة عبر 12 ملفاً)

### 🟡 تغطية المحتوى (فجوات معروفة بالاسم)
- [ ] فجوات محتوى موثقة في الـeval: labor-003 (تقادم)، labor-005 (تراضي)،
      pdpl-005 (متحكم)، evidence-001 (نص إنجليزي مقابل عربي)، multi-001 (بيانات في ملف شركات)
- [ ] محتوى لقطاعات لم تُلمس: العملات الرقمية/المساهمة المبسطة، العمل عن بُعد، تحديثات نطاقات
- [ ] جداول احتساب مكافأة نهاية الخدمة بحالات موضّحة
- [ ] دليل التحكيم التجاري عبر SCCA

### 🟡 جودة الاسترجاع (backlog مقاس)
- [ ] تقييم رفع `max_sections` من 3 إلى 5 (يحد recall الأسئلة متعددة الأقسام:
      companies-001, reac-001, bankrupt-001) — الخيارات: رفع الحد أو ترتيب أفضل
- [ ] أسئلة eval هيكلية بلا حل بلا تعديل مصادر: evidence-001، companies-001
- [ ] إن قرر تجميع حالات عامية كافية: حسم خيارات حساب الثقة الثلاثة أعلاه

### 🟢 دورة البيانات القضائية (هيكل موجود بلا محتوى)
- [ ] `sources/judicial-decisions/` فارغ (README فقط) — لا PDFs في هذا الريبو
- [ ] إطارات `datasets/judicial-index/` و`judicial-reasoning/` جاهزة، تنتظر بيانات

---

## صراحة غير مدرج / Explicitly Out of Scope (الآن)

- **طبقة المنتج** (واجهة ويب، رفع مستندات، تقارير PDF، صلاحيات) — من الريبو الأصلي، مؤجَّل
- **MCP servers خارجية** (boe.gov.sa، uqn.gov.sa، استطلاع) — قرار السيرفر الخالص يمنع الاتصال الخارجي
- **OCR / ingestion pipelines** — خارج نطاق خادم الاسترجاع
- **Vector DB / embeddings** — مرفوضة صراحة لحجم الكوربس (20 مصدراً)
- **بقية اختبارات adversarial** (`test_wrong_article_reference` إلخ) — تُضاف فقط لو أثبتت فجوة غير مغطاة
- **`conflict_detected`** — ميزة بلا طلب مثبت، لا تُبنى

---

## Versioning

| الإصدار | الوصف | الحالة |
|---------|-------|-------|
| v0.2-v0.3 | الأساس: tools + enforce_evidence + manifests | ✅ منجزة |
| v0.4.0-v0.4.16 | كل ما سبق "ما أُنجز" — semver في pyproject ما زال 0.4.0 (أرقام commits علامات داخلية) | ✅ منجزة |
| أول إصدار رسمي | رفع semver + `generate_release_evidence.py` + `verify_release.py` خضراء | ⏳ مخطَّط |

---

## Contribution Priorities

### 🔴 أولوية عالية
- **مراجعة قانونية متخصصة** — محامٍ سعودي مرخّص يراجع دقة المصادر: أثمن مساهمة ممكنة الآن
- **تحديث المصادر** — الأنظمة تتغير: أي تعديل نظامي يبدأ بـ`regulation-index.md` ثم المصدر
- **إغلاق `[يحتاج تحقق]`** — كل علامة مغلقة تحوّل قالباً إلى محتوى فعلي

### 🟡 أولوية متوسطة
- أسئلة eval لسيناريوهات عامية جديدة (كل فشل موثق يغذي مجموعات الترادف)
- بنود dataset لقطاعات ناقصة (عقارات، صحة، تجزئة)
- توثيق أمثلة حية من الاختبارات الحقيقية

### 🟢 أولوية منخفضة
- تحسينات شكلية، ترجمات، أمثلة لمجالات مغطاة

### كيفية المساهمة
1. افتح **Issue** لمناقشة التغيير قبل تنفيذه
2. أي تعديل على `sources/` → `generate_manifests.py` ثم `verify_release.py` (البوابة ستكشف الإهمال)
3. لأي معلومة قانونية: أرفق المرجع الرسمي (رقم النظام، المادة، المصدر)
